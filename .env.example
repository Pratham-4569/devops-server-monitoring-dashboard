# Flask
SECRET_KEY=change-me-in-production
FLASK_ENV=production
FLASK_DEBUG=0

# Alert thresholds (used by backend and frontend in later phases)
CPU_THRESHOLD=80
RAM_THRESHOLD=85
DISK_THRESHOLD=90

# Frontend polling interval in milliseconds
POLL_INTERVAL_MS=3000

# Docker Compose
WEB_PORT=3000
